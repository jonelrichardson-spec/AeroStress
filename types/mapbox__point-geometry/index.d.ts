/** Stub for @types/mapbox__point-geometry (package has no .d.ts). @mapbox/point-geometry provides its own types. */
declare module "@mapbox/point-geometry" {
  export default class Point {
    x: number;
    y: number;
    constructor(x: number, y: number);
  }
}
